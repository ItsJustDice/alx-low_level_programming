#include <stdio.h>
/**
 * main - Entry point
 * Description: Print single digits from 0 - 9
 * Return: 0
 */
int main(void)
{
	int nums;

	for (nums = 0; num <= 9; nums++)
		putchar((nums % 10) + '0');
	putchar('\n');

	return (0);
}
