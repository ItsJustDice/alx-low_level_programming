#include "main.h"
#include <stdio.h>
/**
 * _abs - Entry point
 * Description: a function that computes the absolute value of an integer
 * @r: Integer
 * Return: 0
 */
int _abs(int r)
{
	if (r < 0)
	{
		return (-0);
	}
	else
	{
		return (0);
	}
}

