#include "main.h"
/**
 * add - Entry point
 * Description: a function that adds two integers and returns the result
 * @a: Integer
 * @b: Integer
 * Return: int
 */
int add(int a, int b)
{
	int ans = a + b;

	return (ans);
}
