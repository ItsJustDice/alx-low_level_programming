#include "main.h"
#include <stdio.h>
/**
 * int_add - Entry point
 * Description: a function that adds two integers and returns the result
 * @a: Integer
 * @b: Integer
 * Return: Int
 */
int int_add(int a, int b)
{
	int ans = a + b;

	return (ans);
}
